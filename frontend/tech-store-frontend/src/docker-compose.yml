frontend:
  build: 
    context: ./frontend
    args:
      - VITE_API_URL=http://backend:5000
  ports:
    - "5173:5173"
  environment:
    - VITE_API_URL=http://backend:5000
  depends_on:
    - backend
  networks:
    - app-network